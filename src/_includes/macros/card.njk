{%- macro card(imgSrc, imgAlt, title, subtitle, permalink) -%}
  <div data-component class="c-card">
  <!-- @TODO enter width/height -->
    <img src="{{ imgSrc }}" alt="{{ imgAlt }}" width="" height="" />
    <h3 class="text-xl text-purple-600">{{ title }}</h3>
    <p>{{ subtitle }}</p>
    <a href="{{ permalink }}">Read more ></a>
  </div>
{%- endmacro %}